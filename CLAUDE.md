# Claude Code 项目配置

## 重要配置说明

### 前端服务器配置
- **端口固定为 5173** - 不要变更，用户要求保持固定
- 地址: http://localhost:5173
- 如果端口被占用，需要先杀掉占用进程，然后使用固定端口启动

### 开发服务器启动命令
**前端服务器：**
```bash
npm run dev
```

**后端服务器：**
```bash
python app.py
```

### 项目结构
- 前端框架：React + TypeScript + Vite
- 地图组件：高德地图API集成
- 后端：Python Flask (端口5000)
- 数据库：Supabase PostgreSQL (云端)

### 地图组件说明
- 使用独立DOM容器避免React DOM冲突
- API Key配置在.env文件中: VITE_AMAP_API_KEY
- 组件路径: src/components/RealMap.tsx

### 注意事项
1. 端口5173必须固定，不允许自动切换到其他端口
2. 修改package.json的dev命令已包含--force参数确保端口固定
3. 如遇端口冲突，使用taskkill或powershell停止占用进程

## 用户偏好
- 端口固定5173，每次都要确保使用此端口
- 不需要每次提醒端口问题，应该主动维护

## 开发日志

### 2025-09-16 多轮对话功能实现和UI修复
**原始用户需求：**
1. "你如何实现dify的多轮对话，我期望页面右上角可以添加按钮，新开AI对话页面时，就代表一个新的对话，dify那边就要新开窗口来记录，确保上下文合理，AI对话顶部左侧给一个列表按钮，点击后出现历史对话列表，可以选择对应列表，查看之前的对话记录"
2. 要求只实现P0（核心功能）和P1（重要功能）优先级
3. UI问题："查看历史记录的弹出不对，出现在页面上，应该是在H5上面"
4. UI问题："AI对话顶部 标题固定展示'对话'2字，不要动态取值"

**功能需求：**
1. 实现Dify多轮对话功能（P0和P1优先级）
2. 修复UI层级显示问题
3. 固定对话标题显示

**已完成功能：**
- ✅ P0核心功能：新建对话、对话历史管理、对话切换、Dify上下文管理
- ✅ P1重要功能：对话标题编辑、对话删除、智能标题生成
- ✅ P2功能（第1项）：历史对话列表搜索功能
- ✅ 新增ConversationHistory组件（480+行代码）
- ✅ 集成Dify API多对话支持
- ✅ 数据库conversation表创建和管理

**UI修复：**
- ✅ **弹出层z-index问题** - 将所有弹出层z-index从`z-50`提升到`z-[9999]`，确保在H5页面正确显示
  - ConversationHistory.tsx: 对话历史弹出层
  - LoginModal.tsx: 登录弹出层
  - App.tsx: 删除确认弹窗和通知消息
- ✅ **对话标题固定显示** - 将动态`{conversationTitle}`改为固定显示"对话"文字

**技术实现：**
- 后端新增conversation管理API（创建、获取、更新、删除）
- 前端实现conversation状态管理和UI组件
- Dify API集成每个对话独立的conversation_id
- 智能标题生成基于对话内容
- 搜索功能：实时过滤、关键词高亮、空状态处理

**需求分析和实现过程：**
1. **需求分析阶段**：制定了P0、P1、P2优先级功能清单
   - P0: 新建对话、对话历史管理、对话切换、Dify上下文管理
   - P1: 对话标题编辑、对话删除、智能标题生成
   - P2: 高级功能（未实现）

2. **技术架构设计**：
   - 后端：新增Conversation数据模型和API
   - 前端：实现ConversationHistory组件和状态管理
   - 集成：每个对话独立的Dify conversation_id管理

3. **UI布局实现**：
   - 右上角：新对话按钮（Plus图标）
   - 左上角：历史对话列表按钮（Menu图标）
   - 中央：固定显示"对话"标题

**验证状态：**
- 后端服务运行正常（端口5000）
- 前端服务运行正常（端口5173）
- Dify API连接正常（多个conversation_id验证成功）
- 数据库操作正常（支持多对话存储）
- UI层级显示正常（z-index修复完成）